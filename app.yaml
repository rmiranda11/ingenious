runtime: nodejs14 # or another supported version

instance_class: F2

resources:
memory_gb: 4

handlers:
- url: /stylesheets
  static_dir: stylesheets

- url: /.*
  secure: always
  redirect_http_response_code: 301
  script: auto
